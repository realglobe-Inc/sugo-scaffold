/**
 * Test case runnning by karma.
 */

describe('index', function () {
  it('loads without problems', function () {
    var div = document.createElement('div')
    div.id = 'site'
    document.body.appendChild(div)
  })
})

/* global describe it */
