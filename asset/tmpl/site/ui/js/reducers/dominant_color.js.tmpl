/**
 * Reducer of dominant color string.
 */
const dominantColor = (state = '#ff5', action) => {
  switch (action.type) {
    case 'SET_COLOR':
      return action.color
    default:
      return state
  }
}

export default dominantColor
