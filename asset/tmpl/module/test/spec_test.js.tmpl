/**
 * Test case for spec.
 * Runs with mocha.
 */
'use strict'

const spec = require('../lib/meta/spec.js')
const assert = require('assert')
const co = require('co')

describe('spec', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Spec', () => co(function * () {

  }))
})

/* global describe, before, after, it */
