#!/usr/bin/env node

/** Run caller */
'use strict'

const caller = require('../lib/caller')

caller()
  .then(() => {})
  .catch((err) => {
    console.error('Failed to run caller', err)
    process.exit(1)
  })
