#!/usr/bin/env node

/** Run terminal */
'use strict'

const terminal = require('../lib/terminal')

terminal()
  .then(() => {})
  .catch((err) => {
    console.error('Failed to run terminal', err)
    process.exit(1)
  })
