#!/usr/bin/env node

/** Run actor */
'use strict'

const actor = require('../lib/actor')

actor()
  .then(() => {})
  .catch((err) => {
    console.error('Failed to setup connect actor', err)
    process.exit(1)
  })
