#!/usr/bin/env node

/** Run spot */
'use strict'

const spot = require('../lib/spot')

spot()
  .then(() => {})
  .catch((err) => {
    console.error('Failed to setup connect spot', err)
    process.exit(1)
  })
