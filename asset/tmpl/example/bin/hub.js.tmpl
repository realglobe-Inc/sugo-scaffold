#!/usr/bin/env node

/** Run hub */
'use strict'

const hub = require('../lib/hub')

hub()
  .then((hub) => {})
  .catch((err) => {
    console.error('Failed to start hub', err)
    process.exit(1)
  })

