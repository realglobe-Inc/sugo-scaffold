#!/usr/bin/env node

/** Run cloud */
'use strict'

const cloud = require('../lib/cloud')

cloud()
  .then((cloud) => {})
  .catch((err) => {
    console.error('Failed to start cloud', err)
    process.exit(1)
  })

