/**
 * Test case for configs.
 * Runs with mocha.
 */
'use strict'

const configs = require('../lib/configs.js')
const assert = require('assert')
const co = require('co')

describe('configs', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Configs', () => co(function * () {

  }))
})

/* global describe, before, after, it */
