/**
 * Test case for ui on browser.
 * Runs with karma.
 */
'use strict'

describe('browser', function () {
  it('Browser', function () {
    var div = document.createElement('div')
    div.id = 'mount-root'
    document.body.appendChild(div)
  })
})

/* global describe, before, after, it */
