/**
 * Test case for ui.
 * Runs with mocha.
 */
'use strict'

const ui = require('../lib/ui.js')
const assert = require('assert')
const co = require('co')

describe('ui', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Ui', () => co(function * () {

  }))
})

/* global describe, before, after, it */
