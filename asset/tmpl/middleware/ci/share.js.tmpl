#!/usr/bin/env node

/**
 * Share this project.
 */

'use strict'

process.chdir(`${__dirname}/..`)

const { middlewareShare } = require('sugos-ci')
const pkg = require('../package.json')

middlewareShare(pkg, {})
