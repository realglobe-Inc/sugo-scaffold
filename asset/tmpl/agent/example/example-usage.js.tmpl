'use strict'

const ~~~~package_name@camelcase~~~~ = require('~~~~package_name~~~~')
const co = require('co')

co(function * () {
  let agent = ~~~~package_name@camelcase~~~~('http://example.com')

  {
    // Check if server available
    let ok = yield agent.knock()
    /** ... */
  }
}).catch((err) => console.error(err))
