/**
 * Test case for share.
 * Runs with mocha.
 */
'use strict'

const share = require('../lib/share.js')
const assert = require('assert')
const co = require('co')

describe('share', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Share', () => co(function * () {

  }))
})

/* global describe, before, after, it */
