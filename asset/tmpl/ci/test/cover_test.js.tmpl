/**
 * Test case for cover.
 * Runs with mocha.
 */
'use strict'

const cover = require('../lib/cover.js')
const assert = require('assert')
const co = require('co')

describe('cover', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Cover', () => co(function * () {

  }))
})

/* global describe, before, after, it */
