/**
 * Test case for report.
 * Runs with mocha.
 */
'use strict'

const report = require('../lib/report.js')
const assert = require('assert')
const co = require('co')

describe('report', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Report', () => co(function * () {

  }))
})

/* global describe, before, after, it */
