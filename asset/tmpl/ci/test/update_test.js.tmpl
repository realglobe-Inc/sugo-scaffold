/**
 * Test case for update.
 * Runs with mocha.
 */
'use strict'

const update = require('../lib/update.js')
const assert = require('assert')
const co = require('co')

describe('update', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Update', () => co(function * () {

  }))
})

/* global describe, before, after, it */
