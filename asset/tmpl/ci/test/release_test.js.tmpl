/**
 * Test case for release.
 * Runs with mocha.
 */
'use strict'

const release = require('../lib/release.js')
const assert = require('assert')
const co = require('co')

describe('release', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Release', () => co(function * () {

  }))
})

/* global describe, before, after, it */
