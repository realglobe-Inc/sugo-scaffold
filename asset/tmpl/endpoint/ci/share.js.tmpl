#!/usr/bin/env node

/**
 * Share this project.
 */

'use strict'

process.chdir(`${__dirname}/..`)

const { endpointShare } = require('sugos-ci')
const pkg = require('../package.json')

endpointShare(pkg, {})
